/**
 * @file controll_key.hpp
 * @brief Quiz画面のコントロールボタン識別用
 */
#ifndef CONTROLL_KEY_HPP
#define CONTROLL_KEY_HPP

/**
 * @brief Quiz画面のコントロールボタン識別用
 */
enum class ControllKey
{
    PREVIOUS, /**< 前の問題へ */
    HOME,     /**< Homeに戻る */
    NEXT,     /**< 次の問題へ */
};

#endif
